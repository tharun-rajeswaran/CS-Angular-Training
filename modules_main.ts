import { Circle } from './modules_shapes';

const circle = new Circle(5);
console.log(circle.describe());

//we import the Circle class and create an instance of it, then call its method.
//Modules help keep your code organized and reusable.